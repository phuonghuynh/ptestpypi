machine:
  python:
    version: 2.7

dependencies:
  override:
    - python setup.py develop

test:
  override:
    - echo "ignore"

deployment:
  release:
    branch: staged
    owner: phuonghuynh
    commands:
     - python setup.py sdist bdist_wheel upload
